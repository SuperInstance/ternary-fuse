[package]
name = "ternary-fuse"
version = "0.1.0"
edition = "2021"
description = "Operator fusion for ternary networks — compose operations to eliminate memory round-trips"
license = "MIT"
repository = "https://github.com/SuperInstance/ternary-fuse"
